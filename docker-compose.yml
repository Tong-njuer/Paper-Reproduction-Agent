version: "3.9"

services:
  agent:
    build: .
    container_name: coding-coach-agent
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - ./workspace:/app/workspace
    stdin_open: true
    tty: true