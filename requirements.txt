# ============================================================
# 编程教练 Agent 系统 - 依赖清单
# ============================================================
# 核心框架
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langgraph>=0.0.15  # 可选：用于复杂流程控制

# Agent推理
# 使用的推理机制依赖见 agent/reasoning/ 模块

# 大语言模型提供者（根据需求选择）
openai>=1.0.0
# anthropic>=0.18.0  # 如需使用Claude
# 通义千问
# dashscope>=1.0.0

# 执行环境
docker>=6.0.0  # 代码执行隔离

# Web服务
fastapi>=0.109.0
uvicorn>=0.27.0

# 前端（可选）
gradio>=4.0.0

# 数据与序列化
pydantic>=2.0.0
sqlmodel>=0.0.14  # SQLAlchemy + Pydantic

# 日志与追踪
structlog>=24.0.0
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0

# 工具
tenacity>=8.2.0  # 重试机制

# 测试
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# 开发工具
httpx>=0.26.0
